# TODO - Update drawing ratio

- [ ] Inspect current draw/winner forcing logic in `script.js` (done during planning).
- [ ] Implement weighted target category selection (4:40%, 5:25%, 6:5%, else:3).
- [ ] Update `ensureAtLeastOneWinner()` to force the chosen exact match count (exactly 4/5/6 or exactly 3) by constructing `drawNumbers` accordingly.
- [ ] Ensure forced `drawNumbers` are always 6 unique numbers in 1–99.
- [x] Sanity-check by running the app and drawing repeatedly to confirm match-count frequencies.



# TODO - Ticket buyer + State ID in Previous Drawings

- [ ] Update `recordPreviousDrawing()` to store `stateId` for each winning ticket in `previousDrawings`.
- [ ] Update `updatePreviousDrawingsUI()` to display Buyer name and State ID for each winning ticket (in addition to ticket #, numbers, matches, prize).
- [x] Run a quick sanity check by reading the updated `script.js` and ensuring Previous Drawings render without errors.


